CREATE TABLE REGISTER (
	Email varchar(500) NOT NULL UNIQUE PRIMARY KEY,
    First_Name varchar(500) NOT NULL,
    Last_Name varchar(500) NOT NULL,
    Password_User varchar(500) NOT NULL,
	Register_ID varchar(500) NOT NULL
);


